/**
 * The Main class initializes a new Tetris game and starts it.
 */
class Main {

    /** Initializes a new game and starts it. */
    function void main() {
        var Tetris game;

        let game = Tetris.new();
        do game.run();
        do game.dispose();

        return;
    }
}
